language: java

jdk:
  - oraclejdk8

build:

  ci:
    - gradle build

  on_success:
    - docker build -t="jrobison151/cancellation:$BRANCH.$BUILD_NUMBER" .
    - docker push jrobison151/cancellation:$BRANCH.$BUILD_NUMBER

  integrations:
    hub:
        - integrationName: dhub
          type: docker
          branches:
            only:
              - master

